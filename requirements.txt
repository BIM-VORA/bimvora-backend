# BIMVORA Backend — Python dependencies
# Tested with Python 3.12

# === Web framework ===
fastapi==0.115.5
uvicorn[standard]==0.32.1

# === Data validation ===
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0

# === Database — ASYNC driver for app ===
sqlalchemy==2.0.36
asyncpg==0.30.0

# === Database — SYNC driver for Alembic migrations ONLY ===
# This is the key fix for the MissingGreenlet error:
# Alembic must use psycopg2 (sync), NOT asyncpg
psycopg2-binary==2.9.10
alembic==1.14.0

# === Auth ===
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.12

# === HTTP client (for CAPI calls) ===
httpx==0.28.0

# === Payment ===
stripe==11.3.0

# === Email ===
resend>=2.6.0

# === Storage (Supabase) ===
supabase>=2.10.0

# === Rate limiting ===
slowapi==0.1.9

# === Utils ===
python-dotenv==1.0.1
nanoid==2.0.0

# === Dev / lint ===
ruff==0.8.4
